import { default as React, Fragment} from 'react';
import { useAuth, useApi } from '../services';
import * as Routes from '../routes';

const SjabloonPage = () => {

  return (
    <Fragment>
      
    </Fragment>
  );
};

export default SjabloonPage;